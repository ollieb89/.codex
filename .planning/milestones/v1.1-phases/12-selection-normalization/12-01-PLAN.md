---
phase: 12-selection-normalization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/selector/normalize.js
  - get-shit-done/bin/lib/selector/schema.js
  - get-shit-done/bin/lib/selector/__tests__/normalize.test.js
autonomous: true
requirements:
  - SEL-01
  - SEL-02
must_haves:
  truths:
    - "A numbered list with gaps (1, 3, 7) normalizes to sequential 1, 2, 3 IDs"
    - "0-indexed lists (0, 1, 2) normalize to 1, 2, 3"
    - "Leading zeros (01., 02.) parse correctly and normalize to 1, 2"
    - "Markdown-wrapped numbers (**1.** Option) parse correctly"
    - "Original label text and metadata are preserved after normalization"
    - "Duplicate numbers throw NormalizationError with retry hint"
    - "Empty/unparseable input throws NormalizationError with retry hint"
    - "Exhaustion message includes first 100 chars of raw output"
  artifacts:
    - path: "get-shit-done/bin/lib/selector/normalize.js"
      provides: "normalizeOptions() with full edge-case handling"
      exports: ["normalizeOptions", "NormalizationError"]
    - path: "get-shit-done/bin/lib/selector/schema.js"
      provides: "Updated VALID_NUMBERED_LINE regex and RETRY_BUDGET=2"
      exports: ["VALID_NUMBERED_LINE", "RETRY_BUDGET", "DUPLICATE_HINT", "EMPTY_HINT"]
    - path: "get-shit-done/bin/lib/selector/__tests__/normalize.test.js"
      provides: "Comprehensive normalization test suite"
  key_links:
    - from: "get-shit-done/bin/lib/selector/normalize.js"
      to: "get-shit-done/bin/lib/selector/schema.js"
      via: "require('./schema.js')"
      pattern: "require.*schema"
---

<objective>
Harden normalizeOptions() to handle all AI-generated numbered list formats: gaps, 0-indexing, leading zeros, markdown-wrapped numbers. Update error messages and retry budget per user decisions.

Purpose: SEL-01 (contiguous 1..N IDs) and SEL-02 (preserve original label text/metadata) require the normalizer to be robust against common AI output variations. TDD ensures every edge case is covered before integration.
Output: Updated normalize.js, schema.js, and comprehensive test suite.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-selection-normalization/12-CONTEXT.md
@.planning/phases/12-selection-normalization/12-RESEARCH.md
@get-shit-done/bin/lib/selector/normalize.js
@get-shit-done/bin/lib/selector/schema.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for normalization edge cases</name>
  <files>get-shit-done/bin/lib/selector/__tests__/normalize.test.js</files>
  <action>
Create a comprehensive test file for normalizeOptions() covering all edge cases. Use node:test and node:assert (matching existing test patterns).

Tests to write (all should FAIL against current implementation):

**Gap normalization (SEL-01):**
- Input: entries numbered 1, 3, 7 -> output IDs are 1, 2, 3
- Input: entries numbered 5, 10, 15 -> output IDs are 1, 2, 3

**0-indexed lists:**
- Input: entries numbered 0, 1, 2 -> output IDs are 1, 2, 3

**Leading zeros:**
- Input: "01. First\n02. Second\n03. Third" -> parses and normalizes to IDs 1, 2, 3

**Markdown-wrapped numbers:**
- Input: "**1.** Bold option\n**2.** Another" -> parses correctly, labels are "Bold option" and "Another"
- Input: "`1.` Code option" -> parses correctly

**Label and metadata preservation (SEL-02):**
- After normalization, original label text is unchanged
- After JSON normalization, payload and metadata fields are preserved
- Labels with special characters (parentheses, colons, dashes) are preserved

**Duplicate detection:**
- Input with duplicate numbers throws NormalizationError
- Error has retry=true when attempt < RETRY_BUDGET
- Error hint matches CONTEXT spec: "Duplicate numbered options detected. Re-generate the list with unique sequential numbers (1, 2, 3, ...)."

**Empty/invalid input:**
- Empty string throws NormalizationError
- Non-numbered text throws NormalizationError
- Error hint for empty: present and actionable

**Retry budget:**
- RETRY_BUDGET is 2 (3 total attempts: attempt 0, 1, 2)
- At attempt=2, NormalizationError.retry is false (budget exhausted)
- At attempt=1, NormalizationError.retry is true

**singleOption flag:**
- Single-entry list returns singleOption: true
- Multi-entry list returns singleOption: false

Run RED phase: `node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js` -- most tests should fail.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js 2>&1 | grep -E "^(✔|✗|ℹ)" | tail -5</automated>
    <manual>Verify tests exist and most fail against current implementation (RED phase)</manual>
  </verify>
  <done>Test file exists with 12+ test cases covering all edge cases listed above. Most tests fail because current implementation lacks the features.</done>
</task>

<task type="auto">
  <name>Task 2: Update schema.js and normalize.js to pass all tests</name>
  <files>get-shit-done/bin/lib/selector/schema.js, get-shit-done/bin/lib/selector/normalize.js</files>
  <action>
**schema.js updates:**
- Change `VALID_NUMBERED_LINE` regex to: `/^(?:\*{1,2})?0*(\d+)\.(?:\*{1,2})?\s+(.+)$/` — handles leading zeros and optional bold markdown around the number
- Change `RETRY_BUDGET` to `2` (3 total attempts)
- Update `DUPLICATE_HINT` to: `"Duplicate numbered options detected. Re-generate the list with unique sequential numbers (1, 2, 3, ...)."`
- Keep `EMPTY_HINT` or update to be actionable

**normalize.js updates:**

1. Fix `stripSimpleMarkdown()`: Make it targeted, not global. It should:
   - Strip bold/italic markdown only from the NUMBER PREFIX of lines (e.g., `**1.**` -> `1.`)
   - NOT strip markdown from label content (preserves SEL-02)
   - Handle backtick-wrapped numbers: `` `1.` Option ``
   - Implementation: apply per-line, regex replaces only on the leading number portion

2. Update `parseNumberedLines()`:
   - Use updated `VALID_NUMBERED_LINE` regex that captures the number and text separately
   - Strip leading zeros from captured number: `Number(match[1])` naturally handles this
   - Handle the new regex capture groups

3. Update `normalizeOptions()`:
   - Apply `stripSimpleMarkdown` only to the prefix area, not to labels
   - Ensure `stripAnsi` is applied before parsing (already done)
   - Keep sort + reindex logic (already correct)

4. Update error handling:
   - Add an exhaustion message factory: when `attempt >= RETRY_BUDGET`, the NormalizationError message should be: `"Selection failed: could not parse valid options after ${RETRY_BUDGET + 1} attempts. Raw output was: ${(output || '').slice(0, 100)}. This is usually an AI formatting issue -- try running again."`
   - For attempts below budget, keep existing messages but with updated hints

GREEN phase: Run `node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js` -- all tests should pass.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js && node --test get-shit-done/bin/lib/selector/__tests__/format.test.js && node --test get-shit-done/bin/lib/selector/__tests__/headless.test.js && node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js</automated>
    <manual>All normalize tests pass (GREEN). All existing tests still pass (no regressions).</manual>
  </verify>
  <done>All normalize.test.js tests pass. All existing selector, headless, and format tests pass unchanged. RETRY_BUDGET is 2. VALID_NUMBERED_LINE handles leading zeros and markdown-wrapped numbers. Labels are preserved.</done>
</task>

</tasks>

<verification>
- `node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js` -- all pass
- `node --test get-shit-done/bin/lib/selector/__tests__/format.test.js` -- all pass (no regression)
- `node --test get-shit-done/bin/lib/selector/__tests__/headless.test.js` -- all pass (no regression)
- `node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js` -- all pass (no regression)
</verification>

<success_criteria>
- normalizeOptions() handles gap lists, 0-indexed, leading zeros, markdown-wrapped numbers
- Labels and metadata are preserved through normalization
- Duplicate numbers throw NormalizationError with correct hint
- RETRY_BUDGET is 2 (3 total attempts)
- Exhaustion error includes raw output preview
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/12-selection-normalization/12-01-SUMMARY.md`
</output>
