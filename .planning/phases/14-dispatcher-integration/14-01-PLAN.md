---
phase: 14-dispatcher-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/dispatcher/recovery.js
  - get-shit-done/bin/lib/dispatcher/__tests__/recovery.test.js
autonomous: true
requirements: [ERR-02, ERR-04]
must_haves:
  truths:
    - "Recovery prompt prints a boxed header with exit code, shows the redacted command, stderr hint, and a numbered Retry/Edit/Abort menu."
    - "Same-root-cause detection compares exit code plus command string to decide whether to increment strikes."
  artifacts:
    - path: "get-shit-done/bin/lib/dispatcher/recovery.js"
      provides: "renderRecoveryPrompt, same-root-cause helper, menu constants for dispatcher reuse"
      exports: ["renderRecoveryPrompt", "isSameRootCause", "RECOVERY_CHOICES"]
    - path: "get-shit-done/bin/lib/dispatcher/__tests__/recovery.test.js"
      provides: "node:test coverage for prompt output and root-cause comparison"
  key_links:
    - from: "renderRecoveryPrompt"
      to: "payload.command + payload.stderrHint"
      via: "formatted box with header, command echo, stderr tail, numbered menu lines"
    - from: "isSameRootCause"
      to: "exitCode + command"
      via: "strict equality comparison used by strike counter"
---

<objective>
Build the recovery UI and root-cause helpers that dispatcher integration will call.

Purpose: Provide a reusable, spec-compliant recovery prompt and strike-detection utility before wiring dispatchSelection.
Output: Recovery helper module plus targeted node:test coverage.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ollie/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-dispatcher-integration/14-CONTEXT.md
@.planning/phases/14-dispatcher-integration/14-RESEARCH.md
@.planning/phases/13-foundation/13-02-SUMMARY.md
@get-shit-done/bin/lib/dispatcher/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement recovery helpers</name>
  <files>get-shit-done/bin/lib/dispatcher/recovery.js</files>
  <action>
    - Create a new module exporting renderRecoveryPrompt(payload, opts), isSameRootCause(current, previous), and RECOVERY_CHOICES { retry:'1', edit:'2', abort:'3' }.
    - renderRecoveryPrompt writes the locked GSD checkpoint box: double-line header "COMMAND FAILED (exit code N)", shows "$ {redacted command}", stderr hint body, separator, and numbered menu (1 Retry, 2 Edit, 3 Abort); default output=process.stdout but injectable for tests.
    - isSameRootCause returns true only when both exitCode and command match prior values; safe defaults when previous is null/undefined.
    - Keep helper pure except for render output; no direct stdin usage here.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/dispatcher/__tests__/recovery.test.js</automated>
  </verify>
  <done>
    - recovery.js exports the helpers and menu constants with no lint errors.
    - Prompt rendering matches locked structure and is ready for dispatcher import.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add coverage for prompt and root-cause logic</name>
  <files>get-shit-done/bin/lib/dispatcher/__tests__/recovery.test.js</files>
  <action>
    - Write node:test cases that capture renderRecoveryPrompt output to a writable stream and assert presence/order of header, command echo, stderr hint, and numbered menu.
    - Test RECOVERY_CHOICES mapping and isSameRootCause comparisons (true on same exitCode+command, false when either differs, false when previous absent).
    - Keep tests hermetic (no filesystem), and ensure they import only the new helper module.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/dispatcher/__tests__/recovery.test.js</automated>
  </verify>
  <done>
    - Tests pass locally and fail if prompt format or root-cause logic regresses.
  </done>
</task>

</tasks>

<verification>
- node --test get-shit-done/bin/lib/dispatcher/__tests__/recovery.test.js passes after tasks.
</verification>

<success_criteria>
- Recovery prompt module renders the locked box/menu structure and exports menu constants.
- Root-cause helper returns true only on identical exit code + command pairs.
- Tests cover both rendering and comparison logic and run cleanly with node:test.
</success_criteria>

<output>
After completion, create `.planning/phases/14-dispatcher-integration/14-01-SUMMARY.md`
</output>
