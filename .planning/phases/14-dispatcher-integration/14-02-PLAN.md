---
phase: 14-dispatcher-integration
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - get-shit-done/bin/lib/dispatcher/index.js
  - get-shit-done/bin/lib/dispatcher/__tests__/dispatch-recovery.test.js
  - get-shit-done/bin/lib/dispatcher/recovery.js
autonomous: true
requirements: [ERR-02, ERR-03, ERR-04]
must_haves:
  truths:
    - "Non-zero dispatches print a boxed failure message with exit code, redacted command, stderr tail, and a numbered Retry/Edit/Abort prompt."
    - "Retry and Edit both re-enter dispatchSelection through the full sanitize/preview pipeline; no direct runner shortcuts."
    - "Two consecutive failures with the same root cause trigger an abort after the second attempt, emitting an abort message."
    - "Every dispatch attempt (original, retry, edit) appends a sanitized session record; session write errors never crash dispatchSelection."
    - "Successful retry/edit prints a one-line success confirmation before returning to selection flow."
  artifacts:
    - path: "get-shit-done/bin/lib/dispatcher/index.js"
      provides: "Recovery loop, strike counter, session recording, recursive retry/edit paths"
    - path: "get-shit-done/bin/lib/dispatcher/__tests__/dispatch-recovery.test.js"
      provides: "node:test coverage for retry/edit/abort flows, strike limit, and session writes"
    - path: "get-shit-done/bin/lib/dispatcher/recovery.js"
      provides: "helpers consumed by dispatchSelection for rendering and root-cause detection"
  key_links:
    - from: "dispatchSelection"
      to: "buildRecoveryPayload"
      via: "post-runner failure check that triggers the recovery prompt"
    - from: "dispatchSelection"
      to: "appendRecord"
      via: "try/catch-swallowed session write after every runner execution"
    - from: "Retry/Edit choices"
      to: "dispatchSelection"
      via: "recursive call with updated _recoveryState (strikes, last exitCode, last command)"
    - from: "strike counter"
      to: "abort path"
      via: "isSameRootCause check guarding the 2-strike limit"
---

<objective>
Wire recovery, retries, and session recording into dispatchSelection so users get boxed stderr prompts and safe re-dispatch paths.

Purpose: Fulfill ERR-02/03/04 by integrating Phase 13 leaf modules with the dispatcher control flow.
Output: Updated dispatcher with recovery loop plus integration tests covering retry/edit/abort and session logging.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ollie/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-dispatcher-integration/14-CONTEXT.md
@.planning/phases/14-dispatcher-integration/14-RESEARCH.md
@.planning/phases/13-foundation/13-01-SUMMARY.md
@.planning/phases/13-foundation/13-02-SUMMARY.md
@get-shit-done/bin/lib/dispatcher/index.js
@get-shit-done/bin/lib/session/store.js
@get-shit-done/bin/lib/dispatcher/stderr-bridge.js
@get-shit-done/bin/lib/dispatcher/recovery.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record every dispatch attempt to session store</name>
  <files>get-shit-done/bin/lib/dispatcher/index.js</files>
  <action>
    - Resolve sessionPath defaulting to path.join(cwd, '.planning/session.json') with opts override for tests.
    - After each runner invocation (original, retry, edit), call appendRecord(sessionPath, { command: action.command, exitCode: res.code, stderrSnippet: tail of stderr }) inside try/catch that swallows errors per locked decision.
    - Ensure appendRecord runs before recovery handling so failed attempts are captured even when recovery loops continue.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/dispatcher/__tests__/dispatch-recovery.test.js</automated>
  </verify>
  <done>
    - Session writes occur for every attempt without interrupting dispatcher flow on write errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add recovery loop with strikes and recursive retry/edit</name>
  <files>get-shit-done/bin/lib/dispatcher/index.js</files>
  <action>
    - Use buildRecoveryPayload to detect failures; when non-null, renderRecoveryPrompt then prompt user (ask) with RECOVERY_CHOICES (1 retry, 2 edit, 3 abort).
    - Implement _recoveryState in opts carrying { strikes, lastExitCode, lastCommand }; increment strikes only when isSameRootCause(current, previous) is true, reset when edited command differs.
    - Retry path: re-enter dispatchSelection recursively with same selection/action through full sanitize/preview pipeline; strikes incremented before recursion.
    - Edit path: call editCommand with inline prompt prefilled with the last command; on cancel/Ctrl+C treat as abort; on change, reset strikes; recurse with new command via full sanitize/preview path.
    - Abort path: print abort message and return { ran: true, aborted: true, result } without further retries; enforce hard stop when strikes reach 2 for same root cause.
    - After a successful retry/edit (code 0), print "Command succeeded" one-liner before returning to selection flow.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/dispatcher/__tests__/dispatch-recovery.test.js</automated>
  </verify>
  <done>
    - Non-zero exits surface the boxed prompt; retry/edit paths recurse through dispatchSelection; aborts occur after 2 matching failures.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration tests for recovery flow</name>
  <files>get-shit-done/bin/lib/dispatcher/__tests__/dispatch-recovery.test.js</files>
  <action>
    - Create node:test covering: (a) initial failure then retry succeeds → prints success line, records two session entries; (b) edit produces different command resets strikes and succeeds; (c) two same-cause failures trigger abort with no third prompt; (d) session write errors are swallowed.
    - Stub runner to return canned codes/stdout/stderr; stub ask to feed menu selections and edit input without readline conflicts; capture stdout to assert box/menu and success/abort messages.
    - Use tmp dirs for sessionPath to avoid touching real .planning; clean up after tests.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/dispatcher/__tests__/dispatch-recovery.test.js</automated>
  </verify>
  <done>
    - Tests fail without recovery loop and pass with the implemented behavior, guarding ERR-02/03/04 flows.
  </done>
</task>

</tasks>

<verification>
- node --test get-shit-done/bin/lib/dispatcher/__tests__/dispatch-recovery.test.js passes after tasks.
</verification>

<success_criteria>
- Failed dispatches show the locked recovery prompt and menu with stderr tail and exit code.
- Retry/Edit both route through dispatchSelection (sanitize + preview) and log session entries; strikes reset on edited command change.
- Two matching failures trigger abort, and session write errors never break control flow.
- Integration tests cover retry, edit, abort, and session logging scenarios and run cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/14-dispatcher-integration/14-02-SUMMARY.md`
</output>
