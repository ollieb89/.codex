---
phase: 08-safe-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/dispatcher/sanitize.js
  - get-shit-done/bin/lib/dispatcher/edit.js
  - get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js
autonomous: true
requirements:
  - SAF-02
user_setup: []
must_haves:
  truths:
    - Dispatcher blocks any command/path outside the workspace after resolving real paths; symlinks cannot escape cwd.
    - High-risk commands (e.g., sudo, curl|bash, chown) are blocked; standard dev tools allowed; gray-area remotes (git push, npm publish) require explicit force-dispatch flag.
    - Blocked commands offer inline edit first with optional $EDITOR escape and always re-validate after edits.
    - Previews redact secret-looking env values (e.g., *_API_KEY, *_TOKEN, *_SECRET) while keeping execution values intact.
  artifacts:
    - get-shit-done/bin/lib/dispatcher/sanitize.js
    - get-shit-done/bin/lib/dispatcher/edit.js
    - get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js
  key_links:
    - sanitize.js feeds dispatcher entrypoint to classify/allow/block actions before preview/confirm.
    - edit.js provides inline and $EDITOR-based remediation paths invoked on blocked commands.
---

<objective>
Build the safety “shield” for dispatch: strict workspace boundary, allowlist/blocklist with gray-area force-dispatch gate, secret redaction for previews, and an edit-on-block path.

Purpose: Prevent unsafe or out-of-scope commands from running while giving users a quick way to fix AI-suggested commands safely.
Output: Sanitization + edit helpers with tests proving boundary, allowlist/blocklist, gray-area gating, and redaction.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ollie/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-safe-dispatch/08-CONTEXT.md
@.planning/phases/08-safe-dispatch/08-RESEARCH.md
@.planning/phases/07-schema-selector/7-CONTEXT.md
@.planning/phases/07-schema-selector/7-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Implement workspace-safe sanitizer</name>
  <files>get-shit-done/bin/lib/dispatcher/sanitize.js</files>
  <action>Create helpers to classify actions: resolve real paths for any file args, require they start with cwd, and detect symlink escapes. Maintain allowlist of standard dev tools (git, npm, mkdir, cp, mv, node, npm/yarn/pnpm commands) and blocklist high-risk commands (sudo, curl|bash, chown, dd, mkfs). Mark gray-area remotes (git push, npm publish) as “force-dispatch required”. Return structured result `{status: 'allow'|'block'|'force', reason, sanitizedCommand, redactions}` suitable for dispatcher. Include secret redaction utility that masks values matching `*_API_KEY`, `*_TOKEN`, `*_SECRET` in preview only.</action>
  <verify>Run `node --test get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js` (to be added) to ensure allow/block/force and boundary checks behave as expected.</verify>
  <done>Sanitizer enforces cwd boundary, allow/block/force policy, and produces sanitized command plus redaction metadata.</done>
</task>

<task type="auto">
  <name>Add edit-on-block remediation path</name>
  <files>get-shit-done/bin/lib/dispatcher/edit.js</files>
  <action>Create edit helper that defaults to inline readline prompt for quick fixes and supports Ctrl+E (or flag) to open `$EDITOR` with the blocked command prefilled for multi-line edits. After edit, re-run through sanitizer before proceeding. Return updated command + status or explicit cancel.</action>
  <verify>Manual: simulate blocked command, confirm inline edit path works and editor path is reachable (guard actual editor spawn behind opt-in flag or stub in tests).</verify>
  <done>Blocked commands can be edited inline or via editor; all edits re-validate through sanitizer; user can cancel.</done>
</task>

<task type="auto">
  <name>Test safety shields</name>
  <files>get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js</files>
  <action>Add Node `--test` coverage for: outside-cwd path blocked (including symlink escape); allowlisted commands pass; blocklisted commands blocked; gray-area commands flagged `force`; redaction masks secret patterns in preview output; edited command revalidates. Stub editor invocation to avoid launching real editor.</action>
  <verify>Run `node --test get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js`.</verify>
  <done>Tests cover boundary, allow/block/force logic, redaction, and edit-path revalidation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js
- [ ] Blocked commands require edit/cancel and cannot escape workspace
- [ ] Gray-area commands require explicit force-dispatch flag
- [ ] Secrets redacted in preview but preserved for execution
</verification>

<success_criteria>
- Sanitizer enforces workspace boundary and allowlist/blocklist with gray-area force gate
- Blocked commands route through inline edit with optional editor escape and re-validation
- Secret redaction ensures previews never leak sensitive env values
</success_criteria>

<output>
After completion, create `.planning/phases/08-safe-dispatch/08-01-SUMMARY.md`
</output>
