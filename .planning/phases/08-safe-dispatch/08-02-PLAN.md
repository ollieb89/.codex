---
phase: 08-safe-dispatch
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - get-shit-done/bin/lib/dispatcher/index.js
  - get-shit-done/bin/lib/dispatcher/preview.js
  - get-shit-done/bin/lib/dispatcher/__tests__/dispatcher.test.js
autonomous: false
requirements:
  - SAF-01
user_setup: []
must_haves:
  truths:
    - Dispatcher maps selection payloads to action types (shell/diff/flow) and always shows full raw command/diff before running.
    - Previews highlight destructive terms in red/bold on a dim base, show mini-diff with ±3 lines, and display impact summary + sourceAgentId + action type.
    - Mutating actions require per-action confirmation with safe default cancel; dry-run available and mirrors preview without side effects.
    - Read-only actions can run without extra prompt; non-actionable selections exit immediately.
  artifacts:
    - get-shit-done/bin/lib/dispatcher/index.js
    - get-shit-done/bin/lib/dispatcher/preview.js
    - get-shit-done/bin/lib/dispatcher/__tests__/dispatcher.test.js
  key_links:
    - index.js consumes sanitizer outputs from 08-01 and SelectionResult payloads from Phase 7 selector.
    - preview.js renders full command/diff with redactions and metadata before confirmation/dry-run.
---

<objective>
Build the dispatcher preview + execution flow: high-signal previews (full command, destructive highlighting, mini-diff ±3, metadata), confirmation/dry-run for mutating actions, and integration with sanitizer + selector payloads.

Purpose: Give users transparent visibility and control before execution, honoring safety gates and user decisions.
Output: Dispatcher entrypoint + preview renderer with tests covering previews, confirmations, dry-run, and read-only vs mutating behavior.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ollie/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-safe-dispatch/08-CONTEXT.md
@.planning/phases/08-safe-dispatch/08-RESEARCH.md
@.planning/phases/08-safe-dispatch/08-01-PLAN.md
@.planning/phases/07-schema-selector/7-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Render high-signal previews</name>
  <files>get-shit-done/bin/lib/dispatcher/preview.js</files>
  <action>Implement preview renderer that accepts sanitized action payloads (shell/diff/flow) and outputs: full raw command (no truncation) with dim base, red/bold highlights for destructive verbs/flags; mini unified diff with ±3 context lines for file changes; metadata block showing impact summary (files/paths affected), sourceAgentId, and action type. Apply redactions from sanitizer so secrets never appear in preview. Ensure formatting works for multi-line commands.</action>
  <verify>Run `node --test get-shit-done/bin/lib/dispatcher/__tests__/dispatcher.test.js` (to be added) covering preview rendering and redaction application.</verify>
  <done>Preview shows full raw content with destructive highlighting, mini-diff ±3, and required metadata; secrets redacted.</done>
</task>

<task type="auto">
  <name>Wire dispatcher flow with confirmation/dry-run</name>
  <files>get-shit-done/bin/lib/dispatcher/index.js</files>
  <action>Build dispatcher entry that ingests SelectionResult from selector, skips non-actionable, routes through sanitizer (08-01). If blocked → invoke edit helper; if force-required without `--force-dispatch` flag → stop with clear message. For allowed actions: render preview, then per-action confirmation (default cancel). Mutating actions require explicit confirm; read-only can auto-run. Dry-run prints “Would run:” with preview output, no side effects. Support shell/diff/flow action types, returning structured result (ran, dryRun, cancelled, blocked).</action>
  <verify>Run `node --test get-shit-done/bin/lib/dispatcher/__tests__/dispatcher.test.js` to cover confirmation paths, dry-run behavior, force-dispatch gate, and non-actionable early exit.</verify>
  <done>Dispatcher previews then runs (or dry-runs) actions with correct gating; blocked/force cases handled; non-actionable exits fast.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Approve dispatcher preview UX</name>
  <files>get-shit-done/bin/lib/dispatcher/index.js</files>
  <action>Run dispatcher with sample actions to validate preview/confirm/dry-run UX. Collect feedback on clarity of highlighting, metadata, and prompts.</action>
  <what-built>Dispatcher preview + confirmation/dry-run flow</what-built>
  <how-to-verify>Run a sample mutating command (e.g., `echo \"test\" >> tmp.txt`) through dispatcher: confirm preview shows full command with dim base + red highlights, metadata, and mini-diff for file changes; confirm prompt defaults to cancel; run dry-run and ensure no file writes. Then run a read-only command (`ls`) and confirm it executes without extra prompt. Try a blocked command (`sudo rm -rf /`) and confirm edit/cancel path appears.</how-to-verify>
  <verify>Manual checkpoint; ensure prompts and previews match context decisions.</verify>
  <done>Human verifier approves preview/confirm/dry-run UX or reports issues.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test get-shit-done/bin/lib/dispatcher/__tests__/dispatcher.test.js
- [ ] Manual checkpoint approved for preview + confirmation/dry-run flow
</verification>

<success_criteria>
- Previews match locked rules (full raw command, destructive highlighting, mini-diff ±3, metadata, redactions)
- Confirmation/dry-run gating enforced per action; read-only can run without extra prompt; blocked/force cases handled
- Dispatcher integrates selector payloads and sanitizer/edit helpers
</success_criteria>

<output>
After completion, create `.planning/phases/08-safe-dispatch/08-02-SUMMARY.md`
</output>
