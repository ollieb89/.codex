---
phase: 11-secure-dispatcher
plan: 02
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - get-shit-done/bin/lib/dispatcher/sanitize.js
  - get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "Previews redact OpenAI sk-*, GitHub ghp_*, AWS AKIA*, Stripe sk_live_*, PEM blocks, and connection string credentials"
    - "Secret patterns are applied in specific-to-generic order so provider-prefixed secrets are caught before the generic fallback fires"
    - "Benign values (TOKEN_COUNT=5, file paths containing keyword substrings) are not redacted in previews"
    - "The original, unredacted command is what child_process.exec receives -- redaction affects display only"
    - "Anthropic sk-ant-* keys are redacted by the anthropic pattern, not the openai pattern"
    - "JWT tokens (eyJhbG...) are redacted"
    - "Bearer tokens are redacted"
  artifacts:
    - path: "get-shit-done/bin/lib/dispatcher/sanitize.js"
      provides: "Refactored redactSecrets() using SECRET_PATTERNS from commands.js"
      exports: ["sanitizeAction", "redactSecrets", "isInsideWorkspace", "detectPaths"]
    - path: "get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js"
      provides: "Comprehensive redaction tests covering all provider patterns, false positives, PEM blocks, and display-only verification"
  key_links:
    - from: "get-shit-done/bin/lib/dispatcher/sanitize.js"
      to: "get-shit-done/bin/lib/dispatcher/commands.js"
      via: "require('./commands')"
      pattern: "SECRET_PATTERNS.*isSafeValue.*MIN_SECRET_LENGTH"
    - from: "get-shit-done/bin/lib/dispatcher/index.js"
      to: "get-shit-done/bin/lib/dispatcher/sanitize.js"
      via: "sanitizeAction calls redactSecrets internally; index.js passes action.command (unredacted) to runner"
      pattern: "runner.*action\\.command"
---

<objective>
Refactor redactSecrets() in sanitize.js to iterate SECRET_PATTERNS from commands.js with first-match-wins ordering and safe-value detection, with comprehensive tests proving all success criteria.

Purpose: Deliver the full SEC-01 and SEC-02 requirements -- provider-specific secret redaction with specific-to-generic ordering, false-positive prevention, and display-only redaction.
Output: Refactored sanitize.js; expanded sanitize.test.js with all redaction behavior tests.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-secure-dispatcher/11-RESEARCH.md
@.planning/phases/11-secure-dispatcher/11-01-SUMMARY.md
@get-shit-done/bin/lib/dispatcher/commands.js
@get-shit-done/bin/lib/dispatcher/sanitize.js
@get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js
@get-shit-done/bin/lib/dispatcher/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor redactSecrets() to use SECRET_PATTERNS with ordered iteration</name>
  <files>get-shit-done/bin/lib/dispatcher/sanitize.js</files>
  <action>
1. Add SECRET_PATTERNS, isSafeValue, and MIN_SECRET_LENGTH to the require statement at the top:
   `const { BLOCKED_COMMANDS, ALLOWLIST, matchGray, SECRET_PATTERNS, isSafeValue } = require('./commands');`

2. Replace the existing `redactSecrets(str)` function with a new implementation:

```javascript
function redactSecrets(str) {
  const replacements = [];
  let redacted = str;

  for (const pattern of SECRET_PATTERNS) {
    // Create fresh regex to avoid lastIndex state issues
    const re = new RegExp(pattern.regex.source, pattern.regex.flags);

    if (pattern.name === 'generic_env') {
      // Generic fallback: extract key and value, check if safe
      redacted = redacted.replace(re, (match, key, _suffix, value) => {
        if (!pattern.alwaysRedact && isSafeValue(value)) return match;
        replacements.push({ key, original: value });
        return `${key}=[REDACTED]`;
      });
    } else {
      // Provider/structured patterns: replace entire match with [REDACTED]
      redacted = redacted.replace(re, (match) => {
        replacements.push({ key: pattern.name, original: match });
        return '[REDACTED]';
      });
    }
  }

  return { redacted, replacements };
}
```

Key design decisions:
- Create fresh RegExp from source/flags to avoid `/g` lastIndex state bugs
- Generic fallback preserves key name: `KEY=[REDACTED]`
- All other patterns replace entire match: `[REDACTED]`
- First-match-wins: earlier patterns replace values before later patterns see them
- isSafeValue only applies to generic_env (alwaysRedact: false)

3. Do NOT change any other function in sanitize.js. The exports remain identical.

4. CRITICAL: Verify `index.js` line 99 passes `action.command` (not sanitized) to the runner. This is already correct -- do NOT modify index.js.
  </action>
  <verify>node --check get-shit-done/bin/lib/dispatcher/sanitize.js && node -e "const { redactSecrets } = require('./get-shit-done/bin/lib/dispatcher/sanitize'); const r = redactSecrets('export OPENAI_API_KEY=sk-1234567890abcdef1234567890abcdef'); console.assert(r.redacted.includes('[REDACTED]'), 'should redact'); console.log('OK')"</verify>
  <done>redactSecrets() iterates SECRET_PATTERNS in priority order. Provider patterns replace entire match. Generic fallback preserves key name. Safe values are skipped. All existing sanitizeAction behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive redaction tests to sanitize.test.js</name>
  <files>get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js</files>
  <action>
Append new test sections to sanitize.test.js after existing tests. Do NOT modify existing tests.

**Provider-specific redaction tests (SEC-01):**
- `redacts OpenAI API key (sk-...)`: input `export OPENAI_API_KEY=sk-aBcDeFgHiJkLmNoPqRsTuVwXyZ123456`, verify `[REDACTED]` appears and `sk-aBcD...` is gone
- `redacts GitHub token (ghp_...)`: input `GH_TOKEN=ghp_aBcDeFgHiJkLmNoPqRsTuVwXyZ1234567890`, verify redacted
- `redacts AWS access key (AKIA...)`: input `AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE`, verify redacted
- `redacts Stripe secret key (sk_live_...)`: input `STRIPE_KEY=sk_live_example_key_but_not_real`, verify redacted
- `redacts Anthropic key (sk-ant-...)`: input `ANTHROPIC_API_KEY=sk-ant-api03-aBcDeFgHiJkLmNoPqRsTuVwXyZ`, verify redacted
- `redacts PEM private key block`: input containing `-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----`, verify entire block replaced with single `[REDACTED]`
- `redacts JWT token (eyJhbG...)`: input containing a JWT-shaped string, verify redacted
- `redacts Bearer token`: input `Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.eyJ0ZXN0IjoiMSJ9.abc123`, verify redacted
- `redacts connection string (postgresql://user:pass@host)`: input `DATABASE_URL=postgresql://admin:s3cret@db.example.com:5432/mydb`, verify password not visible
- `redacts mongodb+srv connection string`: input `MONGO_URI=mongodb+srv://user:pass@cluster.example.com/db`, verify redacted

**Ordering tests (SEC-02):**
- `anthropic key matched by anthropic pattern, not openai`: input with `sk-ant-api03-...` key, check replacements[0].key === 'anthropic'
- `generic fallback catches unknown secret type`: input `MY_CUSTOM_SECRET=verylongsecretvalue12345678`, verify it IS redacted with key name preserved
- `generic fallback with key name preserved`: input `MY_TOKEN=longvalue12345678901234`, verify output contains `MY_TOKEN=[REDACTED]`

**False-positive prevention tests (Success Criteria 3):**
- `does NOT redact TOKEN_COUNT=5 (numeric value)`: verify TOKEN_COUNT=5 is unchanged in output
- `does NOT redact SECRET_LENGTH=32 (numeric value)`: verify unchanged
- `does NOT redact API_KEY_FILE=/path/to/key (file path value)`: verify unchanged
- `does NOT redact TOKEN=./relative/path (relative path value)`: verify unchanged
- `does NOT redact short values like TOKEN=abc`: verify unchanged (below MIN_SECRET_LENGTH)

**Display-only redaction test (Success Criteria 4):**
- `sanitizeAction returns redacted sanitizedCommand but preserves original in action`: call sanitizeAction with a command containing a secret, verify `result.sanitizedCommand` contains `[REDACTED]`, verify `result.action.command` contains the original secret

**Multi-secret test:**
- `redacts multiple secrets in one command`: input with both an OpenAI key and a GitHub token in one string, verify both are redacted

**PEM block specifics:**
- `PEM block replaced with single [REDACTED], not per-line`: verify multi-line PEM becomes exactly one `[REDACTED]` token
  </action>
  <verify>node --test get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js 2>&1 | tail -5</verify>
  <done>All new redaction tests pass alongside existing sanitize tests. Provider patterns, ordering, false-positive prevention, and display-only behavior all verified. No regressions in existing tests.</done>
</task>

</tasks>

<verification>
- All existing sanitize.test.js tests still pass (no regressions)
- All existing commands.test.js tests still pass
- All existing dispatcher.test.js tests still pass
- Full test suite: `node --test get-shit-done/bin/lib/dispatcher/__tests__/sanitize.test.js && node --test get-shit-done/bin/lib/dispatcher/__tests__/commands.test.js && node --test get-shit-done/bin/lib/dispatcher/__tests__/dispatcher.test.js`
- Success Criteria 1: Provider patterns redact OpenAI, GitHub, AWS, Stripe, PEM, connection strings
- Success Criteria 2: Anthropic before OpenAI ordering verified
- Success Criteria 3: TOKEN_COUNT=5, file paths, short values NOT redacted
- Success Criteria 4: index.js passes action.command (original) to runner, sanitizedCommand has redactions
</verification>

<success_criteria>
- redactSecrets() uses SECRET_PATTERNS from commands.js with first-match-wins ordering
- All 4 roadmap success criteria tested and passing
- No regressions in existing test suites (39 existing tests)
- display-only redaction confirmed: sanitizedCommand redacted, action.command original
</success_criteria>

<output>
After completion, create `.planning/phases/11-secure-dispatcher/11-02-SUMMARY.md`
</output>
