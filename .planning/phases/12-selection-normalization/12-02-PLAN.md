---
phase: 12-selection-normalization
plan: 02
type: tdd
wave: 2
depends_on:
  - "01"
files_modified:
  - get-shit-done/bin/lib/selector/index.js
  - get-shit-done/bin/lib/selector/__tests__/selector.test.js
  - get-shit-done/bin/lib/selector/__tests__/headless.test.js
autonomous: true
requirements:
  - SEL-03
must_haves:
  truths:
    - "--select=2 resolves to the item at post-normalization position 2 consistently"
    - "GS_DONE_SELECT=2 resolves to the item at post-normalization position 2 consistently"
    - "run() convenience function normalizes then selects in one call"
    - "run() is exported from index.js alongside selectOption"
  artifacts:
    - path: "get-shit-done/bin/lib/selector/index.js"
      provides: "run() convenience function, normalizeOptions re-export"
      exports: ["selectOption", "run", "normalizeOptions", "NormalizationError"]
    - path: "get-shit-done/bin/lib/selector/__tests__/selector.test.js"
      provides: "Tests for run() function"
    - path: "get-shit-done/bin/lib/selector/__tests__/headless.test.js"
      provides: "Tests for headless with normalized entries"
  key_links:
    - from: "get-shit-done/bin/lib/selector/index.js"
      to: "get-shit-done/bin/lib/selector/normalize.js"
      via: "require('./normalize')"
      pattern: "require.*normalize"
    - from: "get-shit-done/bin/lib/selector/index.js"
      to: "get-shit-done/bin/lib/selector/headless.js"
      via: "require('./headless')"
      pattern: "require.*headless"
---

<objective>
Wire normalizeOptions() into the selection pipeline via a run() convenience function in index.js, and ensure headless selection (--select, GS_DONE_SELECT) resolves against post-normalization 1..N IDs.

Purpose: SEL-03 requires that --select and GS_DONE_SELECT reference post-normalization IDs consistently. The run() function (per CONTEXT decision) is the recommended import for most callers.
Output: Updated index.js with run() function, updated tests for both interactive and headless flows.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-selection-normalization/12-CONTEXT.md
@.planning/phases/12-selection-normalization/12-RESEARCH.md
@.planning/phases/12-selection-normalization/12-01-SUMMARY.md
@get-shit-done/bin/lib/selector/index.js
@get-shit-done/bin/lib/selector/normalize.js
@get-shit-done/bin/lib/selector/headless.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for run() and headless normalization</name>
  <files>get-shit-done/bin/lib/selector/__tests__/selector.test.js, get-shit-done/bin/lib/selector/__tests__/headless.test.js</files>
  <action>
**selector.test.js -- add tests for run():**

Add tests (append to existing file, do not modify existing tests):

- `run() normalizes gap list and selects`: Pass raw output "1. Alpha\n3. Beta\n7. Gamma", user types "2" -> returns the Beta entry with id=2
- `run() normalizes 0-indexed list`: Pass raw output "0. Zero\n1. One", user types "1" -> returns Zero entry with id=1
- `run() throws NormalizationError on empty input`: Pass empty string -> throws NormalizationError
- `run() re-exports normalizeOptions and NormalizationError`: Verify `require('../index').normalizeOptions` and `require('../index').NormalizationError` are functions/classes

**headless.test.js -- add tests for normalized entries:**

Add tests (append to existing file):

- `handleHeadless with --select=2 on normalized entries`: Entries have sequential 1..N IDs (post-normalization), --select=2 picks the second item
- `handleHeadless with GS_DONE_SELECT=2 on normalized entries`: Same but via env var
- Integration concept test: raw input with gaps "1. A\n5. B\n9. C" -> normalize to [{id:1},{id:2},{id:3}] -> handleHeadless with --select=2 picks B (now id=2)

RED phase: new tests should fail since run() doesn't exist yet.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js 2>&1 | grep -E "^(✔|✗|ℹ)" | tail -5</automated>
    <manual>New tests for run() fail (RED). Existing tests still pass.</manual>
  </verify>
  <done>New test cases exist for run() and headless normalization. run() tests fail because function doesn't exist. Existing tests unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Implement run() in index.js and update exports</name>
  <files>get-shit-done/bin/lib/selector/index.js</files>
  <action>
Add to index.js:

1. Import normalizeOptions and NormalizationError from './normalize':
   `const { normalizeOptions, NormalizationError } = require('./normalize');`

2. Create `run()` function:
```javascript
/**
 * Normalize raw AI output and present selection.
 * Recommended entry point for most callers.
 * @param {string} rawOutput Raw AI-generated text with numbered options
 * @param {object} opts Options passed to selectOption + normalize
 * @returns {Promise<{id:number,label:string,value:string,actionable:boolean,payload?:object|null,metadata?:object|null}>}
 */
async function run(rawOutput, opts = {}) {
  const { entries, singleOption } = normalizeOptions(rawOutput, { attempt: opts.attempt ?? 0 });
  return selectOption(entries, { ...opts, singleOption });
}
```

3. Update module.exports to include: `selectOption`, `run`, `normalizeOptions`, `NormalizationError`

The key insight: `selectOption()` already works correctly with 1..N sequential entries. `handleHeadless()` inside `selectOption()` uses `entries.find(e => e.id === selection)` which will find items correctly when entries are normalized to 1..N. No changes needed to selectOption or handleHeadless internals.

GREEN phase: Run all tests.
  </action>
  <verify>
    <automated>node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js && node --test get-shit-done/bin/lib/selector/__tests__/headless.test.js && node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js && node --test get-shit-done/bin/lib/selector/__tests__/format.test.js</automated>
    <manual>All tests pass across all four test files. run() works end-to-end. Headless --select resolves against post-normalization IDs.</manual>
  </verify>
  <done>run() function exported from index.js. normalizeOptions and NormalizationError re-exported. --select=2 and GS_DONE_SELECT=2 resolve to post-normalization position 2. All tests pass.</done>
</task>

</tasks>

<verification>
- `node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js` -- all pass including new run() tests
- `node --test get-shit-done/bin/lib/selector/__tests__/headless.test.js` -- all pass including normalization integration
- `node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js` -- all pass (from Plan 01)
- `node --test get-shit-done/bin/lib/selector/__tests__/format.test.js` -- all pass (no regression)
</verification>

<success_criteria>
- run() function exists in index.js and is exported
- normalizeOptions and NormalizationError are re-exported from index.js
- --select=2 resolves to post-normalization position 2
- GS_DONE_SELECT=2 resolves to post-normalization position 2
- All four test files pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-selection-normalization/12-02-SUMMARY.md`
</output>
