---
phase: 09-ux-polish-headless
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/selector/index.js
  - get-shit-done/bin/lib/selector/format.js
  - get-shit-done/bin/lib/selector/__tests__/format.test.js
  - get-shit-done/bin/lib/selector/__tests__/selector.test.js
autonomous: true
requirements:
  - SEL-02
user_setup: []
must_haves:
  truths:
    - Labels truncate at max(40, columns-12) with width-aware measurement; ellipsis appears only when over margin.
    - Number gutter is right-aligned based on the largest index and stays aligned even with wide Unicode or color/no-color mode.
    - Menu rendering remains compact (no blank lines) and respects NO_COLOR/--no-color while preserving structure.
  artifacts:
    - get-shit-done/bin/lib/selector/format.js
    - get-shit-done/bin/lib/selector/index.js
    - get-shit-done/bin/lib/selector/__tests__/format.test.js
    - get-shit-done/bin/lib/selector/__tests__/selector.test.js
  key_links:
    - selector/index.js renders via format helpers to keep gutter alignment and truncation consistent across interactive/headless paths.
---

<objective>
Polish selector readability: width-aware truncation with aligned numbering and no-color fallback so long/wide labels display cleanly and predictably.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ollie/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-ux-polish-headless/9-CONTEXT.md
@.planning/phases/09-ux-polish-headless/9-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Implement width-aware formatting utilities</name>
  <files>get-shit-done/bin/lib/selector/format.js, get-shit-done/bin/lib/selector/__tests__/format.test.js</files>
  <action>Create a formatting module that: computes gutter width from entry count; uses a Unicode-aware width calc (string-width or equivalent); determines margin = max(40, columns-12) with safe default when columns undefined; truncates with ellipsis only when exceeding margin and returns the truncated flag. Add tests covering emoji/CJK labels, narrow/wide terminals, and ellipsis behavior.</action>
  <verify>Run `node --test get-shit-done/bin/lib/selector/__tests__/format.test.js`.</verify>
  <done>Helper returns aligned prefixes and correctly truncated labels across width scenarios.</done>
</task>

<task type="auto">
  <name>Wire selector rendering to formatting helpers</name>
  <files>get-shit-done/bin/lib/selector/index.js, get-shit-done/bin/lib/selector/__tests__/selector.test.js</files>
  <action>Update selector rendering to use the gutter/truncation helpers for numbered lists. Keep compact spacing; right-align numbers; append ellipsis only when truncated. Add tests ensuring alignment for single- and double-digit lists, wide Unicode labels, and NO_COLOR/--no-color disabling ANSI while preserving spacing.</action>
  <verify>Run `node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js`.</verify>
  <done>Selector outputs aligned numbered lists with correct truncation and color fallback while retaining existing selection semantics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test get-shit-done/bin/lib/selector/__tests__/format.test.js
- [ ] node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js
- [ ] Confirm menu output matches CONTEXT decisions (aligned gutter, ellipsis only on truncation, compact layout, NO_COLOR honored)
</verification>

<success_criteria>
- Width-aware truncation obeys max(40, columns-12) and only shows ellipsis when needed.
- Numbering gutter stays aligned for up to double-digit options and wide Unicode labels.
- Selector renders compact lists and respects no-color mode without breaking alignment.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ux-polish-headless/09-01-SUMMARY.md`
</output>
