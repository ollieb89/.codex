---
phase: 09-ux-polish-headless
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - get-shit-done/bin/lib/selector/index.js
  - get-shit-done/bin/lib/selector/headless.js
  - get-shit-done/bin/lib/selector/__tests__/headless.test.js
autonomous: true
requirements:
  - UX-01
  - SEL-02
user_setup: []
must_haves:
  truths:
    - `--select` overrides `GS_DONE_SELECT`; selection `0` exits with code 0; out-of-range exits non-zero (1) and dumps the menu to stderr.
    - Headless mode logs the full menu to stderr in the same multi-line format as interactive and echoes `[Headless] Selected: N (Label)`; stdout remains reserved for action results.
    - Headless respects NO_COLOR/--no-color and uses the same truncation/alignment rules as interactive output.
  artifacts:
    - get-shit-done/bin/lib/selector/headless.js
    - get-shit-done/bin/lib/selector/index.js
    - get-shit-done/bin/lib/selector/__tests__/headless.test.js
  key_links:
    - headless.js shares formatting helpers with selector/index.js to keep menus identical across interactive and scripted flows.
---

<objective>
Add headless preselection with audit-friendly logging and deterministic exits so scripts/CI can drive the selector safely without interactivity.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ollie/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-ux-polish-headless/9-CONTEXT.md
@.planning/phases/09-ux-polish-headless/9-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Implement headless selection handler</name>
  <files>get-shit-done/bin/lib/selector/headless.js, get-shit-done/bin/lib/selector/index.js</files>
  <action>Create a headless helper that accepts entries plus args/env, resolves selection with `--select` overriding `GS_DONE_SELECT`, and returns `{exitCode,result}`. Apply shared formatting to render the menu to stderr; on `0` return exitCode 0; on out-of-range return exitCode 1 after dumping menu. Echo `[Headless] Selected: N (Label)` to stderr for valid selections; keep stdout untouched.</action>
  <verify>Manual dry-run in tests via injected streams to confirm stderr content and exit codes.</verify>
  <done>Headless path matches interactive output, enforces precedence/exit rules, and leaves stdout clean.</done>
</task>

<task type="auto">
  <name>Add headless tests for flags, env, and logging</name>
  <files>get-shit-done/bin/lib/selector/__tests__/headless.test.js</files>
  <action>Cover scenarios: env only, flag overriding env, `--select 0` exits 0, out-of-range exits 1 with menu to stderr, valid selection logs menu + audit line and returns selection, NO_COLOR removes ANSI but keeps alignment. Use stub streams to assert stderr output; ensure truncation/alignment matches interactive helper.</action>
  <verify>Run `node --test get-shit-done/bin/lib/selector/__tests__/headless.test.js`.</verify>
  <done>Headless behaviors are verified with deterministic outputs and exit codes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test get-shit-done/bin/lib/selector/__tests__/headless.test.js
- [ ] Confirm headless menu output matches interactive format and honors NO_COLOR/--no-color
- [ ] Confirm exit codes: 0 for `--select 0`, 1 for out-of-range, success for valid selection with audit echo
</verification>

<success_criteria>
- Headless selection obeys precedence (flag over env), produces audit-friendly stderr logs, and leaves stdout for command results.
- Headless mode shares formatting rules with interactive output, including truncation/alignment and no-color handling.
- Invalid selections are rejected with exit 1 and menu dump; `0` exits cleanly with exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ux-polish-headless/09-02-SUMMARY.md`
</output>
