---
phase: 07-schema-selector
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - get-shit-done/bin/lib/selector/index.js
  - get-shit-done/bin/lib/selector/__tests__/selector.test.js
autonomous: true
requirements:
  - SEL-01
user_setup: []
must_haves:
  truths:
    - Selector renders numbered options with explicit “0: Cancel/Back” and validates numeric input against displayed range.
    - 0 input returns `{ id: 0, label: 'User Cancelled', actionable: false }` and exits current branch immediately.
    - SelectionResult includes required fields (`id`, `label`, `value`, `actionable`) and optional `payload`, `metadata`, with `payload` driving execution when present.
  artifacts:
    - get-shit-done/bin/lib/selector/index.js
    - get-shit-done/bin/lib/selector/__tests__/selector.test.js
  key_links:
    - Selector consumes normalized entries from normalize.js and produces SelectionResult for dispatcher (Phase 8).
---

<objective>
Build the InputSelector helper that renders numbered options, enforces 0-to-exit, validates inputs, and returns the agreed SelectionResult contract for downstream dispatch.

Purpose: Deliver a deterministic selection flow aligned with schema decisions and ready for Phase 8 dispatcher integration.
Output: Selector module + tests covering validation, cancel path, single-option fast path, and metadata/payload handling.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ollie/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-schema-selector/7-CONTEXT.md
@.planning/phases/07-schema-selector/7-RESEARCH.md
@.planning/phases/07-schema-selector/07-01-PLAN.md
@get-shit-done/bin/lib/selector/normalize.js
</context>

<tasks>

<task type="auto">
  <name>Implement selector flow with 0-to-exit</name>
  <files>get-shit-done/bin/lib/selector/index.js</files>
  <action>Build an exported async `selectOption(entries, {input, output, attempt})` that: renders numbered options plus `0: Cancel/Back`; uses readline to accept numeric input; rejects non-numeric/out-of-range with clear error and reprompt; returns `{ id, label, value, actionable, payload?, metadata? }`. If `id === 0`, return `{ id:0, label:'User Cancelled', actionable:false }` immediately. If entries include `singleOption` flag from normalize, bypass list and prompt Y/n to run it.</action>
  <verify>Run `node --print "require('./get-shit-done/bin/lib/selector').selectOption ? 'loaded' : 'missing'"` to ensure module exports.</verify>
  <done>Selector renders list, validates numeric input, supports 0-exit, and returns contract-aligned SelectionResult.</done>
</task>

<task type="auto">
  <name>Honor payload-first execution contract</name>
  <files>get-shit-done/bin/lib/selector/index.js</files>
  <action>Ensure returned SelectionResult sets `actionable` true by default; if an entry has `payload`, include it unchanged. Add option to pass through `metadata` when present. Document via JSDoc that dispatchers must use `payload.command` (when present) as execution source of truth, `label` for display only.</action>
  <verify>Inspect JSDoc/comments to confirm payload/metadata guidance and actionable default behavior.</verify>
  <done>Contract reflects payload-driven execution; metadata is optional passthrough; actionable flag respected.</done>
</task>

<task type="auto">
  <name>Add selector validation tests</name>
  <files>get-shit-done/bin/lib/selector/__tests__/selector.test.js</files>
  <action>Add Node `--test` coverage using mocked input streams for: valid selection; non-numeric retry; out-of-range retry; 0 cancel path; single-option Y/n path; payload present drives `payload` in result; actionable=false entry yields no-op SelectionResult.</action>
  <verify>Run `node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js`.</verify>
  <done>Selector tests cover cancel, validation, payload contract, and single-option flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test get-shit-done/bin/lib/selector/__tests__/selector.test.js
- [ ] Selector enforces 0-to-exit and validates input per CONTEXT decisions
- [ ] SelectionResult fields/contract match locked schema (payload-driven execution, actionable flag)
</verification>

<success_criteria>
- Selector implements strict numeric input with cancel path and single-option fast path
- Returned SelectionResult matches required contract with payload priority for execution
- Tests cover validation, cancel, payload, and single-option scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/07-schema-selector/07-02-SUMMARY.md`
</output>
