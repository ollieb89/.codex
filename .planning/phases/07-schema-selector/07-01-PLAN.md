---
phase: 07-schema-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/selector/schema.js
  - get-shit-done/bin/lib/selector/normalize.js
  - get-shit-done/bin/lib/selector/__tests__/normalize.test.js
  - prompts/snippets/numbered-schema.md
autonomous: true
requirements:
  - SCH-01
  - SCH-02
user_setup: []
must_haves:
  truths:
    - AI option output is enforced to be numbered (1–N) with no filler; non-conforming responses trigger a retry then fail.
    - Normalizer accepts JSON array when present and falls back to numbered text with ANSI/markdown stripped.
    - Normalizer silently re-indexes gaps, rejects duplicates, and supports single-option fast path (Y/n).
  artifacts:
    - get-shit-done/bin/lib/selector/schema.js
    - get-shit-done/bin/lib/selector/normalize.js
    - get-shit-done/bin/lib/selector/__tests__/normalize.test.js
    - prompts/snippets/numbered-schema.md
  key_links:
    - normalize.js consumes schema rules from schema.js and produces SelectionEntry data consumed by selector in later phases.
---

<objective>
Enforce a strict numbered schema for AI outputs and build a resilient normalizer (JSON-first, numbered-text fallback) with retry and fast-path behaviors.

Purpose: Guarantee deterministic option lists for the selector by stripping filler, handling gaps, rejecting duplicates, and honoring the one-retry contract.
Output: Schema snippet + normalization utilities with tests demonstrating required behaviors.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ollie/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-schema-selector/7-CONTEXT.md
@.planning/phases/07-schema-selector/7-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Establish numbered schema snippet</name>
  <files>prompts/snippets/numbered-schema.md</files>
  <action>Create a reusable prompt snippet that enforces the strict numbered format (`1. ...`) with explicit instructions: no filler, start at 1, increment by 1, plain text only. Add guidance text for retries (fix formatting, ensure unique numbering). Document intended consumers (agents/workflows that need numbered lists).</action>
  <verify>Confirm snippet includes regex-friendly instructions and retry hint: `Ensure unique numbering and no filler text`.</verify>
  <done>Snippet exists; instructions cover strict numbering, uniqueness, no filler, and retry hint.</done>
</task>

<task type="auto">
  <name>Implement schema rules and normalization helper</name>
  <files>get-shit-done/bin/lib/selector/schema.js, get-shit-done/bin/lib/selector/normalize.js</files>
  <action>In `schema.js`, export regex/constants for valid lines, duplicate detection, and retry budget (1). In `normalize.js`, implement `normalizeOptions(output, {attempt})` that: strips ANSI/markdown, parses JSON array if present; else extracts numbered lines via `^\d+\.\s+.+$`. Filter filler; hard-fail if zero valid lines. Reject duplicates (hard fail). Re-index gaps to 1..N when ≥2 entries remain. If exactly one entry remains, mark `singleOption=true` for fast-path Y/n. Return structured entries `{id, label, value, actionable: true, payload?, metadata?}` with `retryHint` on failures.</action>
  <verify>Run `node --print "require('./get-shit-done/bin/lib/selector/normalize').testParse && 'ok'"` to ensure module loads; inspect function signatures.</verify>
  <done>Normalizer returns structured entries for valid output; errors include retry hint when appropriate; single-option flag available.</done>
</task>

<task type="auto">
  <name>Add normalization tests for retry and edge cases</name>
  <files>get-shit-done/bin/lib/selector/__tests__/normalize.test.js</files>
  <action>Add Node `--test` coverage for: valid numbered list; filler stripped; JSON array path; gaps re-indexed; duplicate numbers cause failure; zero valid lines triggers retry-needed flag; single-option path flagged; retry budget respected. Keep tests self-contained (no network).</action>
  <verify>Run `node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js`.</verify>
  <done>Tests cover required behaviors and pass locally.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test get-shit-done/bin/lib/selector/__tests__/normalize.test.js
- [ ] Snippet instructions match CONTEXT decisions (strict numbering, uniqueness, retry hint, no filler)
- [ ] Normalizer rejects duplicates and zero-valid-line cases with retry hint, re-indexes gaps, and surfaces single-option fast path
</verification>

<success_criteria>
- Schema snippet enforces strict numbered output with uniqueness and retry guidance
- Normalizer supports JSON-first then numbered-text fallback with sanitization
- Duplicate detection and retry budget implemented (1 retry max)
- Single-option fast path indicated for downstream selector
</success_criteria>

<output>
After completion, create `.planning/phases/07-schema-selector/07-01-SUMMARY.md`
</output>
