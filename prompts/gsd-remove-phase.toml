description = "Remove a future phase from roadmap and renumber subsequent phases"

prompt = """
---
name: gsd-remove-phase
category: gsd
complexity: standard
tools: [read, write, bash, glob]
---


# /gsd-remove-phase - Remove a future phase from roadmap and renumber subsequent phases

## Inputs
- `$ARGUMENTS`: <phase-number>

## Tools
- `read`
- `write`
- `bash`
- `glob`

<objective>
Remove an unstarted future phase from the roadmap and renumber all subsequent phases to maintain a clean, linear sequence.

Purpose: Clean removal of work you've decided not to do, without polluting context with cancelled/deferred markers.
Output: Phase deleted, all subsequent phases renumbered, git commit as historical record.
</objective>

<execution_context>
@/home/ollie/.config/opencode/get-shit-done/workflows/remove-phase.md
</execution_context>

<context>
Phase: $ARGUMENTS

Roadmap and state are resolved in-workflow via `init phase-op` and targeted reads.
</context>

<process>
Execute the remove-phase workflow from @/home/ollie/.config/opencode/get-shit-done/workflows/remove-phase.md end-to-end.
Preserve all validation gates (future phase check, work check), renumbering logic, and commit.
</process>

"""
